# Database

Milestone 0 has no database, schema, database client, migrations, seed data,
repository implementation, or persistence configuration.

This absence is intentional. Database design and implementation belong to a
separately approved milestone and must follow the governing specification before
any database files are introduced.
